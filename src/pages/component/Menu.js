const menu = ({setquiz}) => {
  return (
    <div className="question one">
        <h3>few question to getting into to our nft community</h3>
        <button className="btn" onClick={() => {{setquiz("quiz")}}}>start</button>
    </div>
  )
}

export default menu
